query Users($search: String, $limit: Int, $offset: Int) {
  users(search: $search, limit: $limit, offset: $offset) {
    totalCount
    nodes {
      id
      fullName
      email
      team {
        id
        name
      }
      department {
        id
        name
      }
    }
  }
}